# ⚡ Games-Scraper

> Keyless Steam & Epic game scraping + Weighted KNN personalized recommendations.

## Quick Start (Windows)

```powershell
python -m venv venv
venv\Scripts\Activate.ps1
pip install -r requirements.txt
cd backend
uvicorn main:app --reload
```

Open → http://127.0.0.1:8000

---

